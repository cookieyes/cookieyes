// Stand-in for a strictly necessary tag. Runs before any decision, because `necessary` is
// never gated — the point of having one here beside the tags that are held.
(window.__cyPlaygroundScripts ||= []).push("session-cookie");
