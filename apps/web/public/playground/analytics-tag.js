// A stand-in for an analytics tag, served from this site so the playground makes no
// third-party request of its own. It does nothing but announce that it ran — the whole
// point of the demo is whether it runs at all, and when.
(window.__cyPlaygroundScripts ||= []).push("analytics-tag");
