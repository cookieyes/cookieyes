// Stand-in tag. See session-cookie.js — same idea, gated on its own category so the
// console shows a category gating every script mapped to it, not one script at a time.
(window.__cyPlaygroundScripts ||= []).push("analytics-tag");
