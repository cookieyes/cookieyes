// A stand-in for an advertising tag. See analytics-tag.js — same idea, different
// category, so switching one category off visibly leaves the other alone.
(window.__cyPlaygroundScripts ||= []).push("ad-tag");
