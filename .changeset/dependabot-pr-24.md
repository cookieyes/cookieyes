---
"@cookieyes/cli": minor
---

Update runtime dependency `execa` from 9.6.1 to 10.0.0.

execa 10 requires Node.js 22+, so `@cookieyes/cli` now requires **Node.js >= 22** (raised from >= 20). Node.js 20 reached end-of-life in April 2026.
