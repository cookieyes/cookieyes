# Configuring the CookieYes SDK

**This page now lives on the documentation site:**

## → https://developer.cookieyes.com/docs/getting-started/configuration

Every option with its real type, default and required/optional status — generated from
`sdk/core/src/types.ts`, so it cannot drift from the code.

---

*This file is kept as a pointer rather than deleted: package READMEs published to npm
before the move link here at a fixed path, and those published versions are immutable.*
