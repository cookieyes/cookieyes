# Accessibility

**This page now lives on the documentation site:**

## → https://developer.cookieyes.com/docs/accessibility

Keyboard operability, focus management, screen-reader labelling and reduced motion —
what the SDK guarantees, what the automated suite does and does not cover, and a
checklist for building a custom consent interface.

---

*This file is kept as a pointer rather than deleted: `src/__tests__/a11y.test.tsx`
references it at this path, as do package READMEs published to npm before the move,
and those published versions are immutable.*
