# How script blocking works

**This page now lives on the documentation site:**

## → https://developer.cookieyes.com/docs/components/gated-script

How consent-gated injection works, what it costs, and when you need `networkBlocker`
instead — see also [Network blocking](https://developer.cookieyes.com/docs/network-blocking).

---

*This file is kept as a pointer rather than deleted: package READMEs published to npm
before the move link here at a fixed path, and those published versions are immutable.*
