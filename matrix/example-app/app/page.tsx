// matrix/example-app/app/page.tsx
//
// The interesting behaviour lives entirely in layout.tsx/client-consent.tsx —
// this page only exists because the App Router requires one.
export default function Page() {
  return null;
}
